---
status: done
---

# Day 1 扩展：Ollama 图片入账

## 里程碑

**M0+** — 用本地 Ollama 视觉模型（`qwen2.5vl:7b`）从微信/支付宝截图解析结构化记账 JSON。

## 与 M0 文本链的分工

| 场景 | Provider | 模型 | 示例 |
|------|----------|------|------|
| 自然语言入账 | DeepSeek | `deepseek-chat` | `00_hello_chain.py` |
| 截图入账 | Ollama | `qwen2.5vl:7b` | `01_image_ollama_chain.py` |

## 交付文件

| 文件 | 说明 |
|------|------|
| `examples/01_image_ollama_chain/docker-compose.yml` | Ollama Docker 服务 |
| `examples/01_image_ollama_chain/setup-ollama.sh` | 一键启动 + pull 模型 |
| `examples/01_image_ollama_chain/01_image_ollama_chain.py` | 截图入账 LCEL 链 |
| `common/llm/` | LLM 平台抽象（Provider + Factory） |
| `server/model/request/parsed.py` | 共享 ParsedTransaction / JSON 校验 |

## 运行命令

```bash
# 1. 一键启动 Ollama + 拉模型（首次约 6GB）
./examples/01_image_ollama_chain/setup-ollama.sh

# 2. 运行图片入账示例
.venv/bin/python examples/01_image_ollama_chain/01_image_ollama_chain.py

# 3. 确认 M0 文本链无回归
.venv/bin/python examples/00_hello_chain.py
```

## 验收标准

- [x] `docker compose -f examples/01_image_ollama_chain/docker-compose.yml up -d` 后 Ollama 11434 可访问
- [x] `./examples/01_image_ollama_chain/setup-ollama.sh` 成功 pull `qwen2.5vl:7b`
- [x] `01_image_ollama_chain.py` 解析截图输出合法 `ParsedTransaction`
- [x] `00_hello_chain.py` 文本用例仍全部通过

## 不在本次范围

- PaddleOCR 方案
- FastAPI 上传接口（M1/M5）
- GPU 加速（Mac Docker 无 GPU）
