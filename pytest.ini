[pytest]
testpaths = server/api
python_files = *_test.py
python_functions = test_*
pythonpath = .
addopts = -ra -s

# IDE（Cursor / VS Code）请用「Python: Refresh Tests」重新发现。
# 仅 *_test.py 为测试文件；categories.py / conftest.py 不是测试。
