# Day 1 扩展：Ollama 图片入账

## 里程碑

**M0+** — 用本地 Ollama 视觉模型（`qwen2.5vl:7b`）从微信/支付宝截图解析结构化记账 JSON。

## 与 M0 文本链的分工

| 场景 | Provider | 模型 | 示例 |
|------|----------|------|------|
| 自然语言入账 | DeepSeek | `deepseek-chat` | `00_hello_chain.py` |
| 截图入账 | Ollama | `qwen2.5vl:7b` | `01_image_ollama_chain.py` |

## 交付文件

| 文件 | 说明 |
|------|------|
| `examples/docker-compose.yml` | Ollama Docker 服务 |
| `examples/setup-ollama.sh` | 一键启动 + pull 模型 |
| `examples/01_image_ollama_chain.py` | 截图入账 LCEL 链 |
| `src/common/ollama_llm.py` | Ollama 视觉 LLM 封装 |
| `src/common/transaction_schema.py` | 共享 ParsedTransaction / JSON 校验 |
| `src/common/llm/` | LLM 平台抽象（Provider + Factory） |

## 运行命令

```bash
# 1. 一键启动 Ollama + 拉模型（首次约 6GB）
./examples/setup-ollama.sh

# 2. 运行图片入账示例
.venv/bin/python examples/01_image_ollama_chain.py

# 3. 确认 M0 文本链无回归
.venv/bin/python examples/00_hello_chain.py
```

## 验收标准

- [ ] `docker compose -f examples/docker-compose.yml up -d` 后 Ollama 11434 可访问
- [ ] `./examples/setup-ollama.sh` 成功 pull `qwen2.5vl:7b`
- [ ] `01_image_ollama_chain.py` 解析 `weixinpay01.jpg` 输出合法 `ParsedTransaction`
- [ ] `amount=3000.0`, `merchant` 含「弟弟」, `note` 含「墙纸钱」
- [ ] `00_hello_chain.py` 文本用例仍全部通过
- [ ] `examples/README.md` 含 Ollama 平台介绍与故障排查

## 测试图片

`examples/weixinpay01.jpg` — 微信转账 -3000，说明「剩余墙纸钱」，转给「弟弟」。

## 不在本次范围

- PaddleOCR 方案
- FastAPI 上传接口（M1/M5）
- GPU 加速（Mac Docker 无 GPU）
